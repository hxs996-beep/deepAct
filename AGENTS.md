# DeepAct 开发规则

## 泛化性优先：第一性原理

泛化能力是 DeepAct 方案设计的第一性原理：方案应覆盖一类问题，而不是枚举具体场景。

- 优先提炼一类问题的共同规律（例如"答案是否可被工具验证"），而不是逐个场景打补丁。
- 出现场景枚举（针对某个具体 case 的规则/启发式/分支）是泛化不足的信号，应回到规律本身。
- 判据与通道能覆盖 N 个场景优于 1 个场景；只解决单个场景的机制需要重新设计。
- 给模型能力、由模型自主决定，是覆盖无限场景的通用方式——与"能力优先"一致。

## 能力优先：给模型能力，而不是写死逻辑

设计或修改功能时，优先思考"如何给模型提供能力"——提供工具、通道、可自主访问的信息，让模型基于上下文自行判断和决定；而不是写死判定逻辑或固定模式。

- 一段行为若能由模型自主决定（何时该问用户、何时收敛、如何权衡），就把它表达为能力：工具调用、显式声明通道、可访问的信息。
- 固定模式仅在能力无法表达、且需要确定性保护（安全、数据破坏、不可逆操作）时使用。
- 示例：当模型需要用户输入时，提供显式的提问工具/通道让模型主动发起，而不是用引擎规则猜测"模型何时该问用户"。
