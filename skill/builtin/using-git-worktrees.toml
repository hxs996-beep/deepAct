name = "using-git-worktrees"
description = "Use when starting feature work that needs isolation from current workspace or before executing implementation plans"
keywords = ["worktree", "工作树", "隔离", "分支开发", "git", "分支"]
next_skills = ["subagent-driven-development", "executing-plans"]
content = """
# Using Git Worktrees

## Overview

Ensure work happens in an isolated workspace. Prefer your platform's native worktree tools. Fall back to manual git worktrees only when no native tool is available.

**Core principle:** Detect existing isolation first. Then use native tools. Then fall back to git. Never fight the harness.

**Announce at start:** "I'm using the using-git-worktrees skill to set up an isolated workspace."

## Step 0: Detect Existing Isolation

**Before creating anything, check if you are already in an isolated workspace.**

```bash
GIT_DIR=$(cd "$(git rev-parse --git-dir)" 2>/dev/null && pwd -P)
GIT_COMMON=$(cd "$(git rev-parse --git-common-dir)" 2>/dev/null && pwd -P)
BRANCH=$(git branch --show-current)
```

**Submodule guard:** `GIT_DIR != GIT_COMMON` is also true inside git submodules. Before concluding "already in a worktree," verify you are not in a submodule:

```bash
git rev-parse --show-superproject-working-tree 2>/dev/null
```

**If `GIT_DIR != GIT_COMMON` (and not a submodule):** You are already in a linked worktree. Skip to Step 3.

**If `GIT_DIR == GIT_COMMON` (or in a submodule):** You are in a normal repo checkout.

## Step 1: Create Isolated Workspace

### 1a. Native Worktree Tools (preferred)

If your platform has a worktree creation tool (e.g., `EnterWorktree`, `WorktreeCreate`, `/worktree`), use it and skip to Step 3.

### 1b. Git Worktree Fallback

**Only use this if Step 1a does not apply:**

```bash
# Follow directory priority: existing > global legacy > instruction file > default
# Default to .worktrees/ at the project root

project=$(basename "$(git rev-parse --show-toplevel)")
git worktree add ".worktrees/$BRANCH_NAME" -b "$BRANCH_NAME"
cd ".worktrees/$BRANCH_NAME"
```

**Must verify directory is ignored before creating:** `git check-ignore -q .worktrees`

## Step 3: Project Setup

Auto-detect and run appropriate setup:

```bash
# Node.js
if [ -f package.json ]; then npm install; fi

# Rust
if [ -f Cargo.toml ]; then cargo build; fi

# Python
if [ -f requirements.txt ]; then pip install -r requirements.txt; fi
if [ -f pyproject.toml ]; then poetry install; fi

# Go
if [ -f go.mod ]; then go mod download; fi
```

## Step 4: Verify Clean Baseline

Run tests to ensure workspace starts clean:

```bash
# Use project-appropriate command
npm test / cargo test / pytest / go test ./...
```

**If tests fail:** Report failures, ask whether to proceed or investigate.

**If tests pass:** Report ready.

```
Worktree ready at <full-path>
Tests passing (<N> tests, 0 failures)
Ready to implement <feature-name>
```

## Common Mistakes

- **Fighting the harness:** Using `git worktree add` when platform already provides isolation
- **Skipping detection:** Creating a nested worktree inside an existing one
- **Skipping ignore verification:** Worktree contents get tracked, pollute git status
- **Proceeding with failing tests:** Can't distinguish new bugs from pre-existing issues
"""
